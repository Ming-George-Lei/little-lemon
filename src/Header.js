import logo from './assets/Logo.svg';

const Header = () => {
    return (
        <header>
            <div>
                <img src={logo} alt="Logo"></img>
            </div>
        </header>
    )
}

export default Header;